import 'package:flutter/material.dart';

class KebijakanLayout extends StatelessWidget {
  const KebijakanLayout({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Column(
          
        ),),
    );
  }
}